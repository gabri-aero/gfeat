Installation
============

The GFEATpy library provides a Python interface to the GFEAT C++ source code. It is available via the Python Package Index (PyPI) and can be easily installed using pip. 

.. code-block:: bash

   pip install gfeatpy


